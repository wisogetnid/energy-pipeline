[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "energy-pipeline"
version = "0.1.0"
description = "Energy data processing pipeline"
readme = "README.md"
requires-python = ">=3.8"
license = {text = "MIT"}
authors = [
    {name = "Tobias Vogel", email = "your.email@example.com"},
]
dependencies = [
    "pandas",
    "pyarrow",
    "python-dotenv",
    "matplotlib",
    "requests",
    "python-dateutil",
    "fastparquet",
    "seaborn",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.3.5",
    "pytest-cov>=5.0.0",
    "pylint>=3.0.0",
    "black>=24.0.0",
]

[project.scripts]
energy-pipeline = "pipeline.__main__:main"

[tool.hatch.build.targets.wheel]
packages = ["pipeline"]

[dependency-groups]
dev = [
    "pytest>=8.3.5",
    "pytest-cov>=5.0.0",
    "pylint>=3.0.0",
    "black>=24.0.0",
]
